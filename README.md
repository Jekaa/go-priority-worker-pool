# Priority Worker Pool на Go

[![Go Version](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)](https://golang.org/)

Высокопроизводительный приоритетный пул воркеров на Go с поддержкой трёх уровней приоритета, предотвращением приоритетной инверсии и graceful shutdown. Реализован с использованием продвинутых возможностей языка: `reflect.Select`, `context`, атомарных операций и горутин.

## 📋 Содержание

- [Особенности](#особенности)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [API Reference](#api-reference)
- [Архитектура](#архитектура)
- [Примеры использования](#примеры-использования)
- [Метрики и мониторинг](#метрики-и-мониторинг)
- [Производительность](#производительность)
- [Альтернативные подходы](#альтернативные-подходы)
- [Лицензия](#лицензия)

## ✨ Особенности

- **Три уровня приоритета**: High, Normal, Low
- **Динамическое управление очередями** через `reflect.Select`
- **Временные воркеры** для High-priority задач при переполнении
- **Предотвращение приоритетной инверсии** - Low задачи не блокируют High
- **Graceful shutdown** с корректным завершением всех задач
- **Контекстная поддержка** таймаутов и отмены операций
- **Детальные метрики** выполнения через атомарные счётчики
- **Автомасштабирование** временных воркеров при нагрузке
- **Нулевая аллокация** в критических путях
- **Полное покрытие тестами** (см. `*_test.go`)

## 📦 Установка

```bash
go get github.com/Jekaa/go-priority-worker-pool
```

## 🚀 Быстрый старт

```go
package main

import (
    "context"
    "fmt"
    "time"
    "prioritypool"
)

func main() {
    // Создаём пул с 3 воркерами и очередями разного размера
    pool := prioritypool.NewPriorityPool(3, map[Priority]int{
        HighPriority:   5,
        NormalPriority: 10,
        LowPriority:    20,
    })
    defer pool.Shutdown(context.Background())

    // Отправляем задачи с разными приоритетами
    for i := 0; i < 10; i++ {
        priority := LowPriority
        if i%3 == 0 {
            priority = HighPriority
        }
        
        pool.Submit(context.Background(), priority, func() {
            fmt.Println("Задача выполнена")
            time.Sleep(100 * time.Millisecond)
        })
    }
}
```

## 📚 API Reference

### Типы

```go
type Priority int

const (
    HighPriority Priority = iota
    NormalPriority
    LowPriority
)

type PriorityPool struct { /* ... */ }
```

### Функции

#### `NewPriorityPool(workers int, queueSizes map[Priority]int) *PriorityPool`

Создаёт новый пул воркеров.

- `workers` - количество постоянных воркеров (если 0, используется `runtime.NumCPU()`)
- `queueSizes` - размеры очередей для каждого приоритета

#### `(p *PriorityPool) Submit(ctx context.Context, priority Priority, task func()) error`

Отправляет задачу в пул с указанным приоритетом.

- `ctx` - контекст для таймаута/отмены
- `priority` - приоритет задачи
- `task` - выполняемая функция

Возвращает ошибку если:
- Контекст завершён
- Пул останавливается
- Очередь переполнена (для Non-High задач)

#### `(p *PriorityPool) Shutdown(ctx context.Context) error`

Корректно останавливает пул, дожидаясь завершения всех задач.

- `ctx` - контекст с максимальным временем ожидания

#### `(p *PriorityPool) Metrics() map[string]interface{}`

Возвращает текущие метрики пула:

```go
{
    "submitted":  int64,  // всего отправлено
    "completed":  int64,  // выполнено
    "executing":  int64,  // выполняется сейчас
    "workers":    int64,  // активных воркеров
    "temporary":  int64,  // временных воркеров
    "waiting": map[string]int64{  // ожидающих по приоритетам
        "high":   0,
        "normal": 0, 
        "low":    0,
    },
}
```

## 🏗 Архитектура

### Ключевые компоненты

```
┌─────────────────────────────────────────────────────────────┐
│                      Priority Pool                           │
├───────────────┬──────────────────────────┬──────────────────┤
│  High Queue   │     Dispatcher           │  Worker Pool     │
│  (chan func)  │  (reflect.Select)        │  goroutines      │
├───────────────┼──────────────────────────┼──────────────────┤
│  Normal Queue │  - Приоритезация         │  - Постоянные    │
│  (chan func)  │  - Предотвращение        │  - Временные     │
├───────────────┤    инверсии               │  (TTL 5s)        │
│  Low Queue    │  - Мониторинг             │                  │
│  (chan func)  │  - Метрики                │                  │
└───────────────┴──────────────────────────┴──────────────────┘
```

### Принцип работы

1. **Диспетчер** использует `reflect.Select` для неблокирующего чтения из всех очередей
2. **Порядок приоритетов** задаётся порядком случаев в select
3. **Временные воркеры** создаются для High задач при переполнении
4. **Механизм TTL** автоматически завершает простаивающие временные воркеры
5. **Мониторинг** предотвращает приоритетную инверсию

## 💡 Примеры использования

### 1. Базовое использование с таймаутами

```go
ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
defer cancel()

err := pool.Submit(ctx, HighPriority, func() {
    // Длительная операция
    time.Sleep(10 * time.Second)
})

if err == context.DeadlineExceeded {
    log.Println("Операция превысила таймаут")
}
```

### 2. Обработка тысяч задач

```go
const totalTasks = 10000

var wg sync.WaitGroup
wg.Add(totalTasks)

for i := 0; i < totalTasks; i++ {
    priority := LowPriority
    if i < 100 {
        priority = HighPriority // первые 100 - высокого приоритета
    }
    
    pool.Submit(context.Background(), priority, func() {
        defer wg.Done()
        processItem(i)
    })
}

wg.Wait()
```

### 3. Web-сервер с приоритезацией запросов

```go
http.HandleFunc("/api/high", func(w http.ResponseWriter, r *http.Request) {
    pool.Submit(r.Context(), HighPriority, func() {
        handleHighPriorityRequest(w, r)
    })
})

http.HandleFunc("/api/low", func(w http.ResponseWriter, r *http.Request) {
    pool.Submit(r.Context(), LowPriority, func() {
        handleBackgroundTask(w, r)
    })
})
```

## 📊 Метрики и мониторинг

### Prometheus экспорт

```go
func (p *PriorityPool) PrometheusCollector() {
    metrics := p.Metrics()
    
    prometheus.NewGaugeFunc(prometheus.GaugeOpts{
        Name: "priority_pool_tasks_waiting",
        ConstLabels: prometheus.Labels{"priority": "high"},
    }, func() float64 {
        return float64(metrics["waiting"].(map[string]int64)["high"])
    })
    
    // ... аналогично для других метрик
}
```

### Логирование состояний

```go
go func() {
    ticker := time.NewTicker(10 * time.Second)
    defer ticker.Stop()
    
    for range ticker.C {
        metrics := pool.Metrics()
        log.Printf("Pool stats: %+v", metrics)
        
        if metrics["waiting"].(map[string]int64)["high"] > 100 {
            alert("High queue is growing!")
        }
    }
}()
```

## ⚡ Производительность

### Бенчмарки

```bash
go test -bench=. -benchmem

BenchmarkSubmit/LowPriority-8      1000000    1200 ns/op     0 allocs/op
BenchmarkSubmit/HighPriority-8     1000000    1250 ns/op     0 allocs/op
BenchmarkPoolThroughput-8            50000   35000 ns/op    24 allocs/op
```

### Оптимизации

- **Zero-allocation path** в Submit для основных операций
- **Атомарные операции** вместо мьютексов для счётчиков
- **Пул объектов** для временных структур (при необходимости)
- **Блокировки только** при изменении количества воркеров
